<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item>
        <shape android:shape="ring"
            android:innerRadiusRatio="3"
            android:thickness="8dp"
            android:useLevel="false">
            <solid android:color="@color/match_strong" />
        </shape>
    </item>
    <!-- This is a bit complex to do purely in XML for three segments without custom drawing.
         I will use a simpler approach: a gradient or a custom shape. -->
</layer-list>
