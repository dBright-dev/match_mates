<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="8dp"
    app:cardBackgroundColor="@color/secondary_charcoal"
    app:cardCornerRadius="16dp"
    app:cardElevation="4dp"
    app:strokeColor="@color/bg_champagne"
    app:strokeWidth="0.5dp">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="120dp">

        <!-- Placeholder for background image with champagne tint -->
        <ImageView
            android:id="@+id/ivGroupBg"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:scaleType="centerCrop"
            android:src="@drawable/gradient_overlay"
            android:alpha="0.3"
            android:tint="@color/bg_champagne" />

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="vertical"
            android:padding="16dp">

            <TextView
                android:id="@+id/tvGroupName"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Tech Innovators"
                android:textColor="@color/accent_gold"
                android:textSize="20sp"
                android:textStyle="bold" />

            <TextView
                android:id="@+id/tvMemberCount"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="4dp"
                android:background="@drawable/bg_compatibility_badge"
                android:paddingHorizontal="8dp"
                android:paddingVertical="2dp"
                android:text="8.2k Members"
                android:textColor="@color/white"
                android:textSize="12sp" />

            <View
                android:layout_width="0dp"
                android:layout_height="0dp"
                android:layout_weight="1" />

            <com.google.android.material.button.MaterialButton
                android:id="@+id/btnJoinGroup"
                android:layout_width="wrap_content"
                android:layout_height="36dp"
                android:layout_gravity="end"
                android:backgroundTint="@color/accent_gold"
                android:insetTop="0dp"
                android:insetBottom="0dp"
                android:text="Join Group"
                android:textColor="@color/primary_navy"
                android:textSize="12sp"
                android:textStyle="bold"
                app:cornerRadius="18dp" />

        </LinearLayout>
    </RelativeLayout>
</com.google.android.material.card.MaterialCardView>
